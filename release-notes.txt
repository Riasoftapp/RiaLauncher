RiaLauncher v2.0.0.18

1. Fixed drag & drop of a web browser URL into RiaLauncher. The drop was rejected because only ANSI "Text" was accepted, while browsers provide the URL as UnicodeText / text/uri-list / HTML Format. Now all these formats are recognized and the URL is added as a shortcut.